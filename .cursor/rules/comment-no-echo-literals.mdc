---
description: 注释写意图或标识符，不把可调常量的当前数字抄到别处；改参数不必同步一堆注释
globs: "**/*.cs"
alwaysApply: true
---

# 注释不复述可调字面量

可调阈值、次数、秒、帧率等以 `const` / 字段初值为唯一真源。注释写意图、单位、协作；不把当前数字抄进类摘要、接口 XML、其它方法。仓库既有注释：未改到的不主动清扫。

## 判定（按序）

```
IF 注释紧挨该常量/字段声明
   AND 只写用途、单位、何时生效、与哪个标识符配合
   AND 不把初值再用「N 次/秒」等形式写一遍
   → ALLOW
IF 注释复述某命名常量/阈值的当前值（纯数字或「N 次/秒」）
   AND 该注释不在该声明上
   → FORBID
IF 平台/协议事实（如 API 29），仓库无对应可调常量 → ALLOW
IF 用户明确要求注释写死该数字 → ALLOW
```

## 写法

```csharp
private const int MaxRetry = 3; // 失败后最多再试几次

/// <summary>发送请求；失败则重试。</summary>          // ALLOW
/// <summary>发送请求；失败最多再试 3 次。</summary>   // FORBID：3 已在 MaxRetry

void Send()
{
    // ALLOW：未达上限则再试
    // FORBID：已经失败则再试 3 次
}
```

FORBID 时：删数字。若必须指向参数，写标识符名（`MaxRetry`），不写其当前值。
