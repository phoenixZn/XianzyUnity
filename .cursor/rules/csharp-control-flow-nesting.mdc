---
description: >-
  C# 控制流与嵌套：卫语句、提前返回、条件取反、重复代码提取。编写或审阅 .cs
  时优先遵循，减少大括号嵌套与重复片段。
globs: "**/*.cs"
alwaysApply: false
---

# 控制流、嵌套与重复代码（C#）

编写或修改方法时：

1. **优先卫语句**：对「无效输入、无可执行分支、空引用已处理完毕」等情况，**先处理并 `return`**，不要把剩余逻辑包在深层 `else` 里。
2. **条件取反**：若 `if (ok) { 很长 } else { 短 }`，通常改为 `if (!ok) { 短; return; }` 再接主流程，避免「成功路径」缩进过深。
3. **重复代码提取**：同一逻辑若在 **3 处及以上** 重复出现，且重复体达到 **约 3～10 行** 时，**优先**提取为局部函数或私有方法，避免复制多份；具体行数阈值可由团队约定调整。

**反模式（避免）**：`if (A) { if (B) { ... } else { ... } } else { ... }` 且主逻辑全在最内层。

**推荐模式**：`if (!A) { ...; return; }`，`if (!B) { ...; return; }`，主逻辑在方法后部、顶格或浅缩进。

## 结构映射（供模型对齐，非业务示例）

```csharp
// anti: outer(if inner(if-else) else)
void M(bool c1, bool c2)
{
    if (c1)
    {
        if (c2) { T(); }
        else { U(); }
    }
    else { V(); }
}

// prefer: !c1 -> V+return; !c2 -> U+return; else T (linear tail)
void M(bool c1, bool c2)
{
    if (!c1) { V(); return; }
    if (!c2) { U(); return; }
    T();
}
```
