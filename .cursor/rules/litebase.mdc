---
description: 新增 C# 须补注释；类/public 用 XML；接口/继承用分区锚点（Agent 定位）
globs: "**/*.cs"
alwaysApply: true
---

# 新增功能注释

## 触发

新增功能、功能变更、新增类型、public API、成员变量、非平凡逻辑块 → 必须补注释。

## 风格优先级

1. 默认：`//` 单行，简洁，说明意图/约束/副作用，非复述代码
2. XML（`///`）仅限：
   - `class` / `struct` / `record`
   - `interface`
   - `public` 方法（含 public 属性 accessor 若承载业务语义）
   - 分区锚点第二行（见下；非成员文档）
3. 禁止 XML：成员变量、字段、`private`/`protected`/`internal` 成员、局部变量、实现细节、控制流内联说明

## 详细度

- 类/接口：`///` 说明职责、边界、协作对象
- public 方法：`///` 说明用途、关键参数语义、返回值/异常（如有）
- 成员变量（字段/属性 backing 语义）：`//` 单行，说明用途、单位、生命周期或不变量
- 其余：必要处 `//` 单行；复杂分支/算法/业务规则不可无注释

## 分区锚点（Agent 定位）

用途：横幅+标识符划分成员归属；改契约/基类重载时只动对应分区。机器可读优先，禁止散文式标题。

通用：第 1 行 `/` 横幅（勿夹杂文字）；锚点后连续放该区全部成员，不得跨区穿插。

### 接口实现

触发：类实现 ≥1 个接口且类内有对应成员；额外具体类 API / 非接口块同理。

```csharp
//////////////////////////////////////////////////////////////////////////
/// IViewWrapper:
```

- 第 2 行：`/// ` + 接口名/短 token + `:`

### 继承重载 / 本类

触发：子类含基类 override，或本类自有成员需与基类区分离。

```csharp
//////////////////////////////////////////////////////////////////////////
/// ECWorlds：override

//////////////////////////////////////////////////////////////////////////
/// This：
```

- 基类 override 区：`/// ` + 基类名 + `：override`
- 本类自有成员区：`/// This：`

## 反模式

- 无注释的新增成员变量或非平凡逻辑
- 用 `///` 注释成员变量、private 方法或行内实现
- 注释仅重复标识符名（`// SetHp`）
- 分区缺横幅、标题写叙述句、成员跨区穿插

